import type { Metadata } from 'next'; import Link from 'next/link'; import { notFound } from 'next/navigation'; import { Breadcrumbs } from '@/components/breadcrumbs'; import { JsonLd } from '@/components/json-ld'; import { posts, projects, publishedPosts } from '@/lib/content'; import { createMetadata } from '@/lib/seo'; import { absoluteUrl, site } from '@/lib/site';
export const dynamicParams=false; export function generateStaticParams(){return publishedPosts.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=posts.find(x=>x.slug===slug);return p?createMetadata({title:p.seoTitle,description:p.description,path:`/blog/${p.slug}`,type:'article',noIndex:p.draft,publishedTime:p.published,modifiedTime:p.updated,imageTitle:p.title}):{}}
const id=(h:string)=>h.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');
export default async function PostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=publishedPosts.find(p=>p.slug===slug);if(!post)notFound();const index=publishedPosts.findIndex(p=>p.slug===slug),prev=publishedPosts[index-1],next=publishedPosts[index+1];const relatedProjects=projects.filter(p=>post.relatedProjects.includes(p.slug));return <><div className="shell"><Breadcrumbs items={[{name:'Home',href:'/'},{name:'Blog',href:'/blog'},{name:post.title,href:`/blog/${post.slug}`}]}/></div><JsonLd data={{'@context':'https://schema.org','@type':'BlogPosting','@id':`${absoluteUrl(`/blog/${post.slug}`)}#article`,headline:post.title,description:post.description,image:absoluteUrl(post.featuredImage),datePublished:post.published,dateModified:post.updated,author:{'@type':'Person',name:post.author,url:site.url},publisher:{'@type':'Person',name:site.name,url:site.url},mainEntityOfPage:{'@type':'WebPage','@id':absoluteUrl(`/blog/${post.slug}`)},url:absoluteUrl(`/blog/${post.slug}`),keywords:post.tags.join(', '),articleSection:post.category}}/><header className="page-head"><div className="shell prose"><p className="eyebrow">{post.category}</p><h1>{post.title}</h1><p className="lede">{post.intro}</p><div className="article-meta"><span>By {post.author}</span><span>Published {new Date(post.published).toLocaleDateString('en-GB')}</span><span>Updated {new Date(post.updated).toLocaleDateString('en-GB')}</span></div></div></header><section className="section"><div className="shell case-layout"><article className="prose"><div className="visual" role="img" aria-label={post.featuredImageAlt}><div><span className="eyebrow">{post.category}</span><strong>{post.title}</strong><p>{post.featuredImageAlt}</p></div></div><nav className="toc" aria-label="Table of contents"><strong>On this page</strong><ol>{post.sections.map(s=><li key={s.heading}><a href={`#${id(s.heading)}`}>{s.heading}</a></li>)}</ol></nav>{post.sections.map(s=><section key={s.heading}><h2 id={id(s.heading)}>{s.heading}</h2>{s.paragraphs.map(p=><p key={p}>{p}</p>)}</section>)}{relatedProjects.length>0&&<><h2>Related projects</h2>{relatedProjects.map(p=><p key={p.slug}><Link href={`/projects/${p.slug}`}>See how these ideas shape the {p.title} case study</Link>.</p>)}</>}<div className="author-box"><div className="avatar" aria-hidden="true">VN</div><div><strong>About Viththiyakaran Nadarajah</strong><p>Software developer and IT professional in Newtown, Powys, writing about dependable applications, technical support and useful business technology.</p><Link href="/about">About Viththiyakaran and his working approach</Link></div></div><nav className="post-nav" aria-label="Article navigation">{prev?<Link href={`/blog/${prev.slug}`}>← Previous: {prev.title}</Link>:<span/>}{next&&<Link href={`/blog/${next.slug}`}>Next: {next.title} →</Link>}</nav></article><aside className="side-card"><strong>Article topics</strong><ul>{post.tags.map(t=><li key={t}>{t}</li>)}</ul><a className="button" href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(absoluteUrl(`/blog/${post.slug}`))}`} rel="noopener noreferrer" target="_blank">Share on LinkedIn</a></aside></div></section></>}
