import type { Metadata } from 'next'; import Link from 'next/link'; import { notFound } from 'next/navigation'; import { Breadcrumbs } from '@/components/breadcrumbs'; import { publishedPosts } from '@/lib/content'; import { createMetadata } from '@/lib/seo';
const slugify=(x:string)=>x.toLowerCase().replace(/[^a-z0-9]+/g,'-'); const categories=[...new Set(publishedPosts.map(p=>p.category))];
export const dynamicParams=false; export function generateStaticParams(){return categories.map(category=>({category:slugify(category)}))}
export async function generateMetadata({params}:{params:Promise<{category:string}>}):Promise<Metadata>{const {category}=await params;const name=categories.find(x=>slugify(x)===category);return name?createMetadata({title:`${name} Articles`,description:`Practical ${name.toLowerCase()} articles covering software decisions, supportability and implementation lessons.`,path:`/blog/category/${category}`}):{}}
export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const name=categories.find(x=>slugify(x)===category);if(!name)notFound();const matches=publishedPosts.filter(p=>p.category===name);return <><div className="shell"><Breadcrumbs items={[{name:'Home',href:'/'},{name:'Blog',href:'/blog'},{name,href:`/blog/category/${category}`}]}/></div><header className="page-head"><div className="shell prose"><p className="eyebrow">Blog category</p><h1>{name} articles</h1><p className="lede">Focused, practical writing about {name.toLowerCase()}, with enough context to connect technical choices to users and operations.</p></div></header><section className="section"><div className="shell grid">{matches.map(p=><Link className="card" href={`/blog/${p.slug}`} key={p.slug}><h2>{p.title}</h2><p>{p.description}</p><span className="arrow">Read {p.title.toLowerCase()} →</span></Link>)}</div></section></>}
