import Link from 'next/link'; import { JsonLd } from '@/components/json-ld'; import { publishedPosts } from '@/lib/content'; import { createMetadata } from '@/lib/seo'; import { absoluteUrl, site } from '@/lib/site';
export const metadata=createMetadata({title:'Software Development Blog',description:'Practical articles by Viththiyakaran Nadarajah about .NET and React development, secure architecture, technical support and business software.',path:'/blog'});
export default function Blog(){return <><JsonLd data={[{'@context':'https://schema.org','@type':'Blog','@id':`${absoluteUrl('/blog')}#blog`,url:absoluteUrl('/blog'),name:`${site.name} software development blog`,description:'Practical software development, architecture and technical support articles.',publisher:{'@type':'Person',name:site.name,url:site.url},blogPost:publishedPosts.map(p=>({'@type':'BlogPosting',headline:p.title,url:absoluteUrl(`/blog/${p.slug}`),datePublished:p.published}))},{'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:site.url},{'@type':'ListItem',position:2,name:'Blog',item:absoluteUrl('/blog')}]}]}/><header className="page-head"><div className="shell prose"><p className="eyebrow">Blog</p><h1>Software development and technical support notes</h1><p className="lede">Practical explanations of architecture, security, troubleshooting and business software—written to answer a useful question, not to manufacture search pages.</p></div></header><section className="section"><div className="shell grid">{publishedPosts.map(p=><Link className="card" href={`/blog/${p.slug}`} key={p.slug}><span className="eyebrow">{p.category}</span><h2>{p.title}</h2><p>{p.description}</p><small>Published {new Date(p.published).toLocaleDateString('en-GB',{dateStyle:'long'})}</small><span className="arrow">Read {p.title.toLowerCase()} →</span></Link>)}</div></section></>}
