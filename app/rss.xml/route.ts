import { publishedPosts } from '@/lib/content'; import { absoluteUrl, site } from '@/lib/site';
const escape=(s:string)=>s.replace(/[<>&'\"]/g,c=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[c]!));
export function GET(){const items=publishedPosts.map(p=>`<item><title>${escape(p.title)}</title><description>${escape(p.description)}</description><link>${absoluteUrl(`/blog/${p.slug}`)}</link><guid isPermaLink="true">${absoluteUrl(`/blog/${p.slug}`)}</guid><pubDate>${new Date(p.published).toUTCString()}</pubDate><dc:creator>${escape(p.author)}</dc:creator></item>`).join('');const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/"><channel><title>${escape(site.name)} — Software Development Blog</title><description>${escape(site.description)}</description><link>${site.url}/blog</link><language>en-gb</language>${items}</channel></rss>`;return new Response(xml,{headers:{'Content-Type':'application/rss+xml; charset=utf-8','Cache-Control':'public, max-age=3600, s-maxage=86400'}})}
