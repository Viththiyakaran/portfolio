import type { Metadata } from 'next'; import Link from 'next/link'; import { notFound } from 'next/navigation'; import { Breadcrumbs } from '@/components/breadcrumbs'; import { JsonLd } from '@/components/json-ld'; import { projects, services } from '@/lib/content'; import { createMetadata } from '@/lib/seo'; import { absoluteUrl, site } from '@/lib/site';
export const dynamicParams=false; export function generateStaticParams(){return services.map(({slug})=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services.find(x=>x.slug===slug);return s?createMetadata({title:s.title,description:s.description,path:`/services/${s.slug}`}):{}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);if(!s)notFound();return <><div className="shell"><Breadcrumbs items={[{name:'Home',href:'/'},{name:'Services',href:'/services'},{name:s.title,href:`/services/${s.slug}`}]}/></div><JsonLd data={{'@context':'https://schema.org','@type':'Service',name:s.title,description:s.description,url:absoluteUrl(`/services/${s.slug}`),provider:{'@type':'Person',name:site.name,url:site.url},areaServed:[{ '@type':'AdministrativeArea',name:'Powys'},{'@type':'Country',name:'Wales'},{'@type':'Country',name:'United Kingdom'}]}}/><header className="page-head"><div className="shell prose"><p className="eyebrow">Service</p><h1>{s.title}</h1><p className="lede">{s.description}</p></div></header><section className="section"><div className="shell case-layout"><article className="prose"><h2>Who this is for</h2><p>{s.audience}</p><h2>Problems this service can help solve</h2><ul>{s.problems.map(x=><li key={x}>{x}</li>)}</ul><h2>Typical deliverables</h2><ul>{s.deliverables.map(x=><li key={x}>{x}</li>)}</ul><h2>Simple working process</h2><ol className="process-list"><li><strong>Discover</strong><span>Understand the users, current process and constraints.</span></li><li><strong>Plan</strong><span>Agree a small, testable scope and acceptance criteria.</span></li><li><strong>Build</strong><span>Implement maintainable increments with visible progress.</span></li><li><strong>Review</strong><span>Check accessibility, security, content and operational fit.</span></li><li><strong>Support</strong><span>Provide clear handover, diagnostics and next steps.</span></li></ol><h2>Relevant project evidence</h2><p>Review the <Link href={`/projects/${projects[0].slug}`}>{projects[0].title} security and architecture decisions</Link> or the <Link href={`/projects/${projects[1].slug}`}>{projects[1].title} workflow design case study</Link>.</p><h2>Service area</h2><p>Available for suitable engagements in Newtown, Powys, Mid Wales and wider Wales, as well as remote collaboration across the UK.</p></article><aside className="side-card"><strong>Start a useful conversation</strong><p>Share the current problem, who it affects and the outcome you need.</p><Link className="button" href={`/contact?enquiry=${encodeURIComponent(s.enquiryType)}&subject=${encodeURIComponent(`${s.title} enquiry`)}`}>Enquire about {s.title.toLowerCase()}</Link></aside></div></section></>}
