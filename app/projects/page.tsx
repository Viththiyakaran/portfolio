import Link from 'next/link'; import { projects } from '@/lib/content'; import { createMetadata } from '@/lib/seo';
export const metadata=createMetadata({title:'Software Projects',description:'Detailed .NET, React and business software project case studies by Viththiyakaran Nadarajah, covering architecture, security and lessons learned.',path:'/projects'});
export default function Projects(){return <><header className="page-head"><div className="shell prose"><p className="eyebrow">Projects</p><h1>Software project case studies</h1><p className="lede">Substantial architecture studies that show how I frame requirements, make technical decisions and plan for security, accessibility and support.</p></div></header><section className="section"><div className="shell grid">{projects.map(p=><Link className="card" href={`/projects/${p.slug}`} key={p.slug}><ul className="tags">{p.technologies.map(t=><li key={t}>{t}</li>)}</ul><h2>{p.title}</h2><p>{p.summary}</p><span className="arrow">Read the complete {p.title} case study →</span></Link>)}</div></section></>}
