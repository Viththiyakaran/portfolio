import { createMetadata } from '@/lib/seo';

export const metadata = createMetadata({ title: 'Cookie Notice', description: 'Cookie and analytics consent information for the Viththiyakaran Nadarajah portfolio, including how to change optional analytics preferences.', path: '/cookies' });

export default function CookiesPage() {
  return <><header className="page-head"><div className="shell prose"><p className="eyebrow">Cookies</p><h1>Cookie and storage notice</h1><p className="lede">The site uses a small local preference value and loads optional analytics only after consent.</p></div></header><section className="section"><article className="shell prose"><h2>Essential preference storage</h2><p>The browser stores <code>analytics-consent</code> in local storage so the site remembers whether you accepted or rejected optional analytics. This preference is necessary to respect your choice.</p><h2>Optional Google Analytics</h2><p>If a production GA4 measurement ID is configured and you select Allow analytics, Google Analytics scripts load after your choice. They may measure page views, project and article views, CV downloads, contact success events and outbound links. Sensitive form contents are never included.</p><h2>Rejecting analytics</h2><p>Rejecting analytics does not affect access to any portfolio content or contact options. No optional analytics script is loaded before consent.</p><h2>Changing your preference</h2><p>Use the Cookie settings button fixed near the lower edge of the page. Select Allow analytics or Reject analytics to replace the stored preference.</p><p><small>Last updated: 16 July 2026.</small></p></article></section></>;
}
